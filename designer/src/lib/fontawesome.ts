import { library } from '@fortawesome/fontawesome-svg-core';

// Minimal Font Awesome init: we provide icons directly via <FontAwesomeIcon icon={...} />.
// Keeping this empty avoids importing unavailable icons that cause build failures.
library.add();

export {};
